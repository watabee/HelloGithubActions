name: Hello, World!
on: push

jobs:
  build:
    name: Run Unit Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
        with:
          fetch-depth: 1
      - run: touch /home/runner/.android/repositories.cfg && ./gradlew testDebugUnitTest
